# 모바일(안드로이드) 앱 안에 싣는 파이썬 패키지. Chaquopy 가 설치한다.
# 데스크톱 전용(pyinstaller 등)은 뺐다. 네이티브 확장(pycryptodome)은 Chaquopy 가
# 안드로이드용으로 빌드해 둔 버전만 쓸 수 있어서 버전을 고정하지 않는다 (MarkupSafe, pycryptodome).
Flask==3.1.0
Werkzeug==3.1.3
Jinja2==3.1.5
MarkupSafe
itsdangerous==2.2.0
blinker==1.9.0
click==8.1.8
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30
charset-normalizer
idna==3.8
pycryptodome
